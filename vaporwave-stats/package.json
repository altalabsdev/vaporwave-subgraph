{
  "name": "vaporwave",
  "license": "MIT",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy-prod": "graph deploy --product hosted-service vaporwave/vaporwave-stats",
    "deploy-test": "graph deploy --product hosted-service vaporwave/vaporwave-stats-stagin --deploy-key $(cat .access_token)"
  },
  "dependencies": {
    "@graphprotocol/graph-ts": "^0.27.0",
    "ethers": "^5.4.6",
    "moment": "^2.29.1"
  }
}
