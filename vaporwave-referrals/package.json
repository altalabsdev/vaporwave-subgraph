{
  "name": "vaporwave-referrals",
  "license": "MIT",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy-test": "",
    "deploy-prod": "graph codegen && graph deploy --product hosted-service vaporwave/vaporwave-referrals"
  },
  "dependencies": {
    "@graphprotocol/graph-ts": "^0.27.0"
  }
}
